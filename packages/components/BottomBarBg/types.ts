export interface BottomBarBgProps {
  totalWidth?: number; // 整体宽度
  totalHeight?: number; // 整体高度
  curveWidth?: number; // 曲线宽度
  curveHeight?: number; // 曲线高度
  arcRadius?: number; // 圆弧半径
  activeIndex?: number; // 当前活动的导航索引
  navCount?: number; // 导航项总数
}